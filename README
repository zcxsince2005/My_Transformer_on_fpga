# 用 AI 在 FPGA 上复现 Transformer 大模型

本目录是本次大作业的完整工程提交目录，包含 Transformer/nanoGPT FPGA 主工程、LeNet-5 额外上板工程、CPU/GPU 基准测试、QuestaSim RTL 仿真和三平台性能能效对比图。

目标平台：

```text
Zynq UltraScale+ MPSoC xczu7ev-ffvc1156-2-i
Vivado / Vitis / Vitis HLS 2024.2
```

## 1. 当前目录结构

```text
My_Transformer_on_fpga/
├── README.md
├── 三平台性能能效对比.png
├── cpu_and_gpu_benchmark/
├── My_lenet_on_fpga/
├── My_Transformer_on_fpga/
└── sim/
```

## 2. 当前路径下文件和子路径说明

| 文件/子路径 | 内容说明 |
|---|---|
| `README.md` | 当前总说明文件，用于介绍整个提交目录和各子工程。 |
| `三平台性能能效对比.png` | CPU、GPU、FPGA 三个平台的性能和能效对比图，用于报告和答辩展示。 |
| `cpu_and_gpu_benchmark/` | nanoGPT 在 CPU/GPU 上的推理性能基准测试脚本、运行结果和截图。 |
| `My_lenet_on_fpga/` | LeNet-5 MNIST 卷积网络 FPGA 部署工程，作为额外工作展示。 |
| `My_Transformer_on_fpga/` | 主线 Transformer/nanoGPT FPGA 工程，包含 Python 模型导出、HLS IP、Vivado 工程和 Vitis 裸机程序。 |
| `sim/` | QuestaSim/ModelSim RTL 仿真目录，保存仿真脚本、日志、覆盖率和波形截图。 |

## 3. 主线 Transformer 工程

主工程位于：

```text
My_Transformer_on_fpga/
```

它采用三段式结构：

```text
python_model_export/
HLS/
vivado2024.2/
```

主要内容：

- `python_model_export/`：字符级 nanoGPT 模型训练、INT8 量化、权重导出、tokenizer 和 golden 数据。
- `HLS/`：Vitis HLS C/C++ kernel、testbench、综合脚本、HLS 工程和导出 IP。
- `vivado2024.2/`：Vivado Block Design、bitstream/XSA、Vitis 裸机 PS 端 UART 程序。
- `结果截图/`：低资源/高资源 Vivado 结果、串口上板验证、矩阵计算验证、tiny 模型验证等截图。

主线模型配置：

```text
模型: character-level nanoGPT / causal Transformer
数据集: tiny Shakespeare
n_layer = 6
n_head = 6
n_embd = 384
block_size = 256
vocab_size = 65
量化: INT8
```

已完成验证内容：

- Python 端模型训练、量化和导出流程。
- Vitis HLS 多个加速核生成。
- Vivado 集成 ZynqMP PS、AXI SmartConnect 和 HLS IP。
- Vivado 综合、实现、bitstream、XSA。
- Vitis 裸机串口菜单和 PS 端 IP 调度。
- UART、AXI-Lite 寄存器访问、矩阵乘硬件计算和文本交互演示。

## 4. LeNet 额外工程

额外工程位于：

```text
My_lenet_on_fpga/
```

该工程使用 TensorFlow/Keras 和 hls4ml 将 LeNet-5 部署到同一 FPGA 平台，用于 MNIST 手写数字识别。它包含：

- `HLS/`：LeNet-5 训练、hls4ml 转换和测试图片头文件。
- `lenet5_hls_5ns/`：Vitis HLS 工作区。
- `lenet5_hls_local/`：Vivado/Vitis 工程。
- `结果截图/`：LeNet 资源占用、时延、GPU 对比、数字验证图片等结果。

## 5. CPU/GPU 基准

基准测试目录位于：

```text
cpu_and_gpu_benchmark/
```

该目录用于对同一 nanoGPT 模型在 CPU 和 GPU 上的逐 token 推理速度进行测试，为 FPGA 结果提供对照。

当前保存结果：

```text
CPU: Intel 8 核 INT8 路径, 平均约 58.43 ms/token
GPU: NVIDIA GeForce RTX 4090, 平均约 2.78 ms/token
```

## 6. RTL 仿真

仿真目录位于：

```text
sim/
```

该目录使用 QuestaSim/ModelSim 对 HLS 生成的 RTL 做验证：

- 11 个 PL/HLS 模块编译、细化和复位运行通过。
- `tb_mac_muladd` 对 INT8 MAC 单元做功能自检，9 组向量全部通过。
- 保存覆盖率 `.ucdb`、文本报告、仿真日志和波形截图。

## 7. 推荐阅读顺序

建议按以下顺序查看：

1. 当前 `README.md`。
2. `My_Transformer_on_fpga/README.md`。
3. `My_Transformer_on_fpga/python_model_export/README.md`。
4. `My_Transformer_on_fpga/HLS/README.md`。
5. `My_Transformer_on_fpga/vivado2024.2/README.md`。
6. `My_Transformer_on_fpga/结果截图/README.md`。
7. `cpu_and_gpu_benchmark/README.md`。
8. `sim/README.md`。
9. `My_lenet_on_fpga/README.md`。
